<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        android:layout_marginTop="16dp">

        <!-- Картинка фигуры -->
        <ImageView
            android:layout_width="128dp"
            android:layout_height="128dp"
            android:src="@drawable/rectangle"/>

        <!-- Формулы для фигуры -->
        <ImageView
            android:layout_width="128dp"
            android:layout_height="128dp"
            android:src="@drawable/rectangle_formula"/>

        <!-- Поля ввода -->
        <EditText
            android:id="@+id/input_width"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:inputType="numberDecimal"
            android:hint="Ширина прямоугольника"
            android:layout_marginTop="32dp"/>

        <EditText
            android:id="@+id/input_height"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:inputType="numberDecimal"
            android:hint="Высота прямоугольника"
            android:layout_marginTop="16dp"/>

        <!-- Кнопка расчета -->
        <Button
            android:id="@+id/btn_calculate"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Рассчитать"
            android:layout_marginTop="32dp"/>

        <!-- Вывод результатов -->
        <TextView
            android:id="@+id/output_perimeter"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Периметр: "
            android:layout_marginTop="16dp"/>

        <TextView
            android:id="@+id/output_area"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Площадь: "/>

        <!-- Поделиться результатом -->
        <Button
            android:id="@+id/btn_share_results"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Поделиться результатами"
            android:layout_gravity="center_horizontal"
            android:layout_marginTop="32dp"/>

    </LinearLayout>
</ScrollView>